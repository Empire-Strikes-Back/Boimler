{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Boimler reporting for duty",
			"type": "extensionHost",
			"request": "launch",
			"runtimeExecutable": "${execPath}",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}"]
		}
	]
}
