{
	"name": "mult",
	"description": "clojure/script extension",
	"version": "0.0.0",
	"engines": {
		"vscode": "^1.30.0"
	},
	"activationEvents": [
		"onLanguage:clojure",
		"onCommand:mult.spec/cmd-open"
	],
	"main": "./resources/out/mult-vscode/main.js",
	"dependencies": {
		"nrepl-client": "0.3.0"
	},
	"devDependencies": {
		"antd": "4.9.2",
		"react": "17.0.1",
		"react-dom": "17.0.1",
		"shadow-cljs": "2.11.7",
		"vsce": "1.87.0",
		"vscode": "1.1.37"
	},
	"contributes": {
		"commands": [
			{
				"command": "mult.spec/cmd-open",
				"title": "Open mult"
			},
			{
				"command": "mult.spec/cmd-ping",
				"title": "ping"
			},
			{
				"command": "mult.spec/cmd-eval",
				"title": "Eval current form"
			},
			{
				"command": "mult.spec/cmd-format-current-form",
				"title": "Format current form"
			},
			{
				"command": "mult.spec/cmd-select-current-form",
				"title": "Select current form"
			}
		],
		"keybindings": [
			{
				"command": "mult.spec/cmd-open",
				"key": "ctrl+alt+m ctrl+alt+o"
			},
			{
				"command": "mult.spec/cmd-ping",
				"key": "ctrl+alt+m ctrl+alt+p"
			},
			{
				"command": "mult.spec/cmd-eval",
				"key": "alt+w",
				"when": "editorTextFocus && editorLangId == 'clojure' && !editorReadOnly && !hasOtherSuggestions && !inSnippetMode && !suggestWidgetVisible"
			},
			{
				"command": "mult.spec/cmd-format-current-form",
				"key": "ctrl+alt+m ctrl+alt+f",
				"when": "editorTextFocus && editorLangId == 'clojure' && !editorReadOnly && !hasOtherSuggestions && !inSnippetMode && !suggestWidgetVisible"
			},
			{
				"command": "mult.spec/cmd-select-current-form",
				"key": "ctrl+w",
				"when": "editorTextFocus && editorLangId == 'clojure' && !editorReadOnly && !hasOtherSuggestions && !inSnippetMode && !suggestWidgetVisible"
			}
		],
		"languages": [
			{
				"id": "clojure",
				"aliases": [
					"Clojure",
					"clojure"
				],
				"extensions": [
					".clj",
					".cljs",
					".cljc",
					".cljx",
					".clojure",
					".edn"
				]
			}
		]
	}
}
